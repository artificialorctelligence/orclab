jacoco.xml — from documented schema — verify on first real run.

Hand-built against the documented JaCoCo XML report format (the same format Kover for Kotlin
writes), not captured from a real tool run. Verify its shape against a real JaCoCo report the
first time one is actually generated.
