# calc.x_clamp__mutmut_1: survived
--- src/calc/__init__.py
+++ src/calc/__init__.py
@@ -1,5 +1,5 @@
 def clamp(x, lo, hi):
-    if x < lo:
+    if x <= lo:
         return lo
     if x > hi:
         return hi
