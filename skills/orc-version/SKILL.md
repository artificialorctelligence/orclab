---
name: orc-version
description: Use when the user explicitly asks to use orc-version, or types /orc-version, to set or increment the current project's version, draft a changelog entry, tag the commit, or cut a release.
---

# orc-version

Read the file at `${CLAUDE_SKILL_DIR}/../../commands/orc-version.md` in full, and follow its
instructions exactly, using the same request/arguments this invocation received as `$ARGUMENTS`.
