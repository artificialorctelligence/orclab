---
name: orc-code
description: Use when the user explicitly asks to use orc-code, or types /orc-code, to start a new project, add a feature to an existing project, or refactor/migrate existing code.
---

# orc-code

Read the file at `${CLAUDE_SKILL_DIR}/../../commands/orc-code.md` in full, and follow its
instructions exactly, using the same request/arguments this invocation received as `$ARGUMENTS`.
