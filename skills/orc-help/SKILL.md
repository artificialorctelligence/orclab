---
name: orc-help
description: Use when the user explicitly asks to use orc-help, or types /orc-help, to see Orclab's own running version and a synopsis of its available commands.
---

# orc-help

Read the file at `${CLAUDE_SKILL_DIR}/../../commands/orc-help.md` in full, and follow its
instructions exactly, using the same request/arguments this invocation received as `$ARGUMENTS`.
