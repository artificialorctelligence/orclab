---
name: orc
description: Use when the user explicitly asks to use orc, or types /orc - an alias for orc-help, showing Orclab's own running version and available commands.
---
# /orc

Read the file at `${CLAUDE_SKILL_DIR}/../orc-help/SKILL.md` in full, and follow its
instructions exactly, using the same `$ARGUMENTS` this invocation received.
