---
name: orc-git
description: Use when the user explicitly asks to use orc-git, or types /orc-git, for git/GitHub shortcuts - connecting a repo, committing, pushing, branching, or checking out a PR.
---

# orc-git

Read the file at `${CLAUDE_SKILL_DIR}/../../commands/orc-git.md` in full, and follow its
instructions exactly, using the same request/arguments this invocation received as `$ARGUMENTS`.
